<%= form_for(@archtype) do |f| %>
  <% if @archtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@archtype.errors.count, "error") %> prohibited this archtype from being saved:</h2>

      <ul>
      <% @archtype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <h2>Type[s]</h2>
  <div class="field">
    <%= label :type, :name %><br />
    <%= text_field :type, :name %>
  </div>
  <div class="field">
    <%# label :type, :description %><br />
    <%# text_area :type, :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
